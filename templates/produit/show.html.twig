{% extends 'base.html.twig' %}

{% block title %}Produit
{% endblock %}

{% block body %}
	<div class="container-fluid col-12">
		<div class="row">
			<div class="col-5 my-5 ms-5">
				<img src="{{asset('img/produits/' ~ produit.picture)}}" class="img-thumbnail img-size" alt="">
			</div>
			<div class="col my-5 ms-5 bg-light">
				<h1 class="ms-3 mt-3">{{produit.name}}</h1>
				<p class="fs-1 ms-3 mt-3">{{produit.price}}
					€</p>
				<p class="fs-2 ms-3 mt-3">Description :</p>
				<p class="ms-3">{{produit.description}}</p>
				<div class="text-center">
					<a href="{{path("app_cart_add", {id: produit.id})}}" class="btn btn-secondary ">
						Ajouter au panier
					</a>
				</div>
			</div>
		</div>
	</div>
	<a class="btn" href="{{ path('app_produit') }}">back to list</a>

	<a class="btn" href="{{ path('app_produit_edit', {'id': produit.id}) }}">edit</a>

	{{ include('produit/_delete_form.html.twig') }}
{% endblock %}
