{% extends 'base.html.twig' %}

{% block title %}Connexion
{% endblock %}

{% block body %}
	{% if error %}
		<div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
	{% endif %}
	<h1 class="text-center my-5">Connexion</h1>
	{#<div class="container text-center">
					<form action="{{ path('app_login') }}" method="post">
						<label for="username">Email:</label>
						<input type="text" id="username" name="_username" value="{{ last_username }}">
			
						<label for="password">Mot de passe:</label>
						<input
						type="password" id="password" name="_password">
			
						{# If you want to control the URL the user is redirected to on success <input type="hidden" name="_target_path" value="/account"> #}

	{#<button class="btn" type="submit">Connexion</button>
					</form>
					<div class="row">
						<p>Pas inscrit ?
							<a href="{{ path('app_register') }}">S'inscrire</a>
						</p>
					</div>
				</div>#}

	<form class="m-3" action="{{ path('app_login') }}" method="post">
		<div class="container text-center bg-light my-5">
			<div class="row justify-content-center">
				<div class="mb-3 col-6">
					<label class="mt-3" for="username">E-mail</label>
					<input type="text" id="username" name="_username" value="{{ last_username }}" class="form-control" aria-describedby="idHelp" placeholder="">
				</div>
			</div>
			<div class="row justify-content-center">
				<div class="mb-3 col-6">
					<label class="mt-3" for="password">Mot de passe</label>
					<input type="password" id="password" name="_password" class="form-control" id="password" aria-describedby="passwordHelp" placeholder="">
				</div>
			</div>
			<div class="row">
				<a class="mt-2" href="{{ path('app_register') }}">S'inscrire</a>
			</div>
			<button type="submit" class="btn btn-outline-secondary btn-lg m-3" value="submit">Connexion</button>
		</div>
	</form>
{% endblock %}
